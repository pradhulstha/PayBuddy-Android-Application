<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prash.paybuddy">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_State" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AccountActivity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".PaymentDetails" />
        <activity android:name=".AccountActivity.SignupActivity" />
        <activity android:name=".AccountActivity.Reset_password" />
        <activity android:name=".AccountActivity.Account_setting" />
        <activity android:name=".SavingCard" />
        <activity android:name=".AccountActivity.save_pay" />
        <activity android:name=".AccountActivity.display" />
        <activity android:name=".AccountActivity.card_choose" />
        <activity android:name=".AccountActivity.credit_display"></activity>
        <activity android:name=".AccountActivity.gift_card_display"/>
    </application>

</manifest>